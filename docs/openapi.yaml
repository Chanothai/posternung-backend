# ⚠️ ไฟล์นี้ไม่ใช่ API contract อีกต่อไป — เป็น pointer เท่านั้น
#
# contract จริงย้ายไปที่:
#     ../../workspace/docs/api/openapi.yaml
#
# ทำไมถึงย้าย: mobile กับ backend เคยไม่มีอะไรผูกให้ endpoint ตรงกัน (แอปก๊อป path
# เป็น string มือ) — ย้ายมาไว้ที่ workspace เพื่อให้มี source of truth ชุดเดียวที่ทั้ง
# สองฝั่งอ้างถึงได้ ดู workspace/CLAUDE.md
#
# ทำไมไฟล์นี้ยังอยู่: ยังมีที่อื่นอ้างถึง path นี้อีก 10 จุด (skill poster-database,
# comment ใน app/, postman collection) การลบทิ้งจะทำให้ reference เหล่านั้นห้อย
#
# ห้ามแก้ไฟล์นี้ · ห้ามเอาไปใช้เป็น spec · ห้าม generate client จากไฟล์นี้
# ต้องการ spec จริง → ../../workspace/docs/api/openapi.yaml
# ต้องการสิ่งที่โค้ดทำจริง → ../openapi.json (generate จาก FastAPI) ใช้เทียบ drift ได้
#
# หมายเหตุ: path ใน contract ที่มี `x-status: DRAFT` = ออกแบบไว้แต่ยังไม่มีโค้ดจริง
# ห้าม implement โดยไม่ผ่านขั้น contract ของ skill `feature`

openapi: 3.1.0
info:
  title: "MOVED — see ../../workspace/docs/api/openapi.yaml"
  version: "0.0.0"
paths: {}
