"""ค่าคงที่ที่เทสหลายไฟล์ใช้ร่วมกันหลัง ADR-0028 (marketplace)

🔴 **ทำไมเทสเดิม ~49 จุดต้องเติม 2 keyword** ‹2026-08-22 · INF-32›

`posters` ได้ข้อบังคับใหม่สองข้อที่บังคับระดับ DB:

1. `seller_id` **NOT NULL** — ADR-0028 D3 · **ตั้งใจไม่มี `server_default`**
   การลืมระบุผู้ขายต้องเป็น error ที่ดังทันที ไม่ใช่การยกของให้ร้านเราเงียบ ๆ
   (ในตลาดที่มีผู้ขายหลายเจ้า การเดาเจ้าของคือความผิดพลาดที่แพงที่สุดที่ทำได้)
2. `ck_posters_sellable_requires_approved_at` — BR-L6 ทุก listing ต้องผ่านแอดมิน
   อนุมัติก่อนขึ้นขาย · แถวที่ `status` เป็น `available`/`reserved`/`sold`
   จึงต้องมี `approved_at` เสมอ

**ห้ามอ่านการเติมสองค่านี้ว่า "เทสถูกดัดให้ผ่าน"** — เทสพวกนั้นสร้างแถวที่อ้างว่า
พร้อมขาย แถวที่พร้อมขายจริงต้องมีเจ้าของและต้องผ่านการอนุมัติ · การที่เทสเดิม
ไม่ต้องระบุคือผลของโลกที่มีผู้ขายเจ้าเดียว ซึ่งจบไปแล้วที่ ADR-0028
"""

from datetime import datetime, timezone

from app.models.seller import HOUSE_SELLER_ID

# เวลาที่ใช้แทน "แถวนี้ผ่านการอนุมัติแล้ว" ในเทส — ค่าคงที่ ไม่ใช่ `now()`
# เพื่อให้เทสที่เทียบค่าตรง ๆ ไม่แกว่งตามเวลาที่รัน
HOUSE_APPROVED_AT = datetime(2026, 1, 1, tzinfo=timezone.utc)

__all__ = ["HOUSE_APPROVED_AT", "HOUSE_SELLER_ID"]
